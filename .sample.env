PINECONE_API_KEY=your-pinecone-key
OPENAI_API_KEY=your-openai-key
LIVEKIT_URL=your-livekit-url
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRECT=your-live-api-secrect

# Add any other environment variables you need here
# Example:
# MY_CUSTOM_VARIABLE=my-value
# Ensure to replace the placeholders with your actual keys and URLs.
# Note: Do not commit this file to version control as it contains sensitive information.
# You can create a .env file in your project root and load it using dotenv in your Python code.
# Ensure to keep this file secure and do not share it publicly.
# Example of how to load this .env file in Python:
# import os
# from dotenv import load_dotenv
# load_dotenv()  # This will load the variables from .env file into the environment
